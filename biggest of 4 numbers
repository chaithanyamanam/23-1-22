#include <stdio.h>
int main()
{
    int a,b,c,d;
    printf("enter a,b,c,d values");
scanf("%d%d%d%d",&a,&b,&c,&d);
if(a>b&&a>c&&a>d)
printf("a is big");
if(b>a&&b>c&&b>d)
printf("b is big");
if(c>a&&c>b&&c>d)
printf("c is big");
else if(d>a&&d>b&&d>c)
printf("d is big");
    return 0;
}
